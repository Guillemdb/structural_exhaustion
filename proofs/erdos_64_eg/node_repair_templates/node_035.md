# Node [35] repair template

Incoming `[33]`; outgoing `[36]`. Sole responsibility: extract the finite rank-reducing dependence/circuit prescribed by the admitted rank-drop certificate and retain its selected inclusion-minimal support/determination certificate for the subsequent original-interface audit. Inherit Branch D and the node-[31] rank witnesses. Use CT15 pair-circuit extraction on the live yes cursor; do not perform node [36]'s context decision. `P13Node33To35` is reusable only for certificate field requirements after deleting legacy handoff plumbing and its `False.elim` shortcut.

- `N35-CIRCUIT`: `node35PairCircuit` invokes CT15's proof-level
  `pairCircuitOfRankDrop` on the proof-carrying manuscript rank and the literal
  node-[32] proof. It never invokes the later full-rank contradiction.
- `N35-FUNCTIONAL`: the selected proof-carrying CT15 candidate retains the
  carrier determination for that exact circuit.
- `N35-SUPPORT`: `Node35Output.supportCertificate` retains the exact
  support-stratified determination certificate for the circuit's quotient
  code, two coordinates, and final carrier. It is projected directly through
  `CT15.SupportStratifiedRank.Profile.certificate` from the selected candidate.
- `N35-SCOPE`: the retained certificate contains connectedness, support
  inclusion, boundary restriction, carrier determination, representative,
  and minimality. Node [36] alone decides whether its restriction is valid
  against every original-atom outside context.
- `N35-ROUTE`: `Node35Stage` uses the reusable framework mapper over node
  [33]'s yes continuation. Node [34] and the outer bypass remain literal.
- `N35-WORK`: `node35LocalChecks = 0`; no quotient, support, context, or graph
  universe is enumerated.
- `N35-RUN`: `runInitialThroughNode35` extends the literal node-[34] runner
  and accepts only the initial residual; Core retrieves the predecessor stage.

Every listed obligation is kernel-checked. Node [35]'s local responsibility is
unconditional and its producer has no `sorryAx`. No original-interface context
decision, support-location split, representative decision, separated-tester
normalization, or global closure is assigned to node [35].
