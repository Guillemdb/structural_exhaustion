# Node [42] repair template

## Paper identity

| Field | Value |
|---|---|
| Node | `[42]` |
| Incoming edge | `[41]` yes (`Z ⊊ G`) |
| Outgoing edge | branch-local terminal |
| Local responsibility | Close proper-support smearing by the target-defect/compression alternative. On this exact path the quotient is already admitted and final-carrier context-universal, so the surviving alternative is its stored CT3 compression. |
| Retained facts | Exact node-[41] proper proof, admitted CT15 candidate, represented-reduction clause, and distinct identified coordinate pair. |
| New output | Contradiction on the proper constructor only. |
| Framework operation | `State.StageNode.closeFocusedBranchYes`. |

## Obligations

| Task ID | Paper assertion | Incoming producer | Lean source | Outgoing edge | Status |
|---|---|---|---|---|---|
| N42-PROV | Consume only node [41]'s literal proper constructor. | `[41]` | `Node42Stage`; `node42P13ProperSupportSmearingClosure` requires `Node41Stage` | terminal | kernel-checked, conditional |
| N42-UNIVERSAL | The target-defect side is absent on this path because the final-carrier proposal is already admitted/context-universal. | `[35]` admitted candidate | `circuit.candidate.admissible` | terminal | kernel-checked, conditional |
| N42-REPRESENTATIVE | A non-injective admitted proposal on a proper carrier supplies the literal represented CT3 reduction. | `[35]` admission; `[41]` proper | `Admissible.injective_of_originalEligible` | terminal | kernel-checked, conditional |
| N42-COLLISION | Injectivity contradicts the distinct coordinate collision selected by CT15. | `[35]` | `node42ProperSupportImpossible`; `PairCircuit.distinct`; `PairCircuit.identified` | terminal | kernel-checked, conditional |
| N42-ROUTE | Eliminate only the proper leaf; preserve the whole leaf and every opaque bypass. | framework | `closeFocusedBranchYes` | `[43]` sibling remains | kernel-checked, conditional |
| N42-WORK | The closure is proof-level and performs zero local scans. | framework/graph | `node42LocalChecks = 0` | terminal | kernel-checked, conditional |
| N42-RUN | Accumulate the terminal certificate without rebuilding the residual. | `[41]` runner | `runInitialThroughNode42` | terminal | kernel-checked, conditional |

## Status

Node [42] is locally kernel-checked but remains partial solely because its
incoming ledger depends on the accumulated node-[21] framework ledger,
`Node23DenseWindowQuietBlockInput`, and
the missing node-[35] collision-support obligation. Its local producer showed no
`sorryAx`. Node [42] makes no whole-carrier, repair-network, or global-profile
claim.
