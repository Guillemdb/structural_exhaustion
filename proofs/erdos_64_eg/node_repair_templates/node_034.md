# Node [34] repair template

Incoming `[32] no`; outgoing `[47]`. Sole responsibility: name Residual B by
selecting the literal no constructor already produced at node [32]. The local
output is `PUnit`; the complementary near-full certificate `W₂-ε ≤ rΩ`
remains only in node [32]'s framework decision carrier and is not copied into
node [34]. The current certified finite error is zero, but no equality is made
a new branch or node-[34] obligation. Forbidden: curvature cost [48], entropy,
or any Branch-D work.

- `N34-FULL`: `Node34Output := PUnit`; `node34P13FullRankResidual` selects and
  names the existing node-[32] no constructor without proving or copying any
  new mathematics. The `Node32FullRank` proof remains in that constructor.
- `N34-ROUTE`: `Node34Stage` is the independent framework-owned no
  continuation of node [32]. It is appended after node [33] from the same
  accumulated state without consuming or rebuilding node [33].
- `N34-WORK`: `node34LocalChecks = 0`.
- `N34-RUN`: `runInitialThroughNode34` extends the literal node-[33] runner,
  while the node-[34] executor retrieves node [32] from that ledger.

Every listed obligation is kernel-checked, conditional. Node [34] remains
yellow solely because its incoming ledger depends on
the accumulated node-[21] framework ledger and `Node23DenseWindowQuietBlockInput`. Its local
producer showed no `sorryAx`. Node [34] owns no curvature-cost, entropy,
support, or Branch-D closure theorem.
