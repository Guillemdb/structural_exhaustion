# Red-team audit: node [144] parallel repair ledger

## Baseline

- Repair sketch: `node_144_parallel_repair_ledger.md`.
- Read-only specification hash: `original_erdos_64_proof.tex` =
  `215248521b7dbc6519076d506101b80a1c9a8425ee5c13b48a4ec52df21d2e10`.
- Failed node and claim: [144], the total implication from one repeated coarse
  code and two actual routing germs to a named sparse exit or decorated Type-B
  handoff.
- Methodology: branch states, C1--C5, CT3, CT7, CT10, both-sides admission,
  level-2/3 repair, leaf totality, and local-computation audit.
- Verdict: **FAIL for the full node-[144] repair; PASS for the newly isolated
  high-separator support slice.**  No manuscript merge or green status is
  permitted.

## Provenance matrix

| Used fact | Producer | Earlier? | Independent? | Verdict |
|---|---|---:|---:|---|
| Exact overloaded token--role fibre | nodes [137], [139], [141] | yes | yes | admissible |
| Literal 49-entry matching/star | nodes [140], [142], [143] | yes | yes | admissible |
| First equal 48-code pair and actual germs | current node-[144] prefix | yes | yes | admissible prefix |
| Five-way attachment/prefix/divergence table | node [177] | yes | yes | admissible |
| Exact root/after-edge incidences | node [178] graph classifier | yes | yes | admissible |
| Cubic-star geometry | current normalization/decomposition | yes | yes | geometry only |
| High port table and pair distinctness | `HighSeparatorPortClassification` | yes | yes | admissible |
| Four-cycle exclusion | activated surplus setup | yes | yes | admissible |
| Complete Type-B assignment | none | no | no | unavailable |
| Complete finite response vector for prefix/cubic pieces | none | no | no | unavailable |

## Quantifier attack

| Claim | Literal obligation | Countermodel/test | Result |
|---|---|---|---|
| attachment mismatch gives target | every unequal row constructs a target cycle | a path with unequal pendant attachments is a tree | false shortcut; first coordinate retained |
| prefix gives compression | every prefix pair has equal response in all compatible contexts and a smaller admissible representative | path-prefix equality contains neither statement | unproved; CT3/C2 trigger absent |
| cubic claw gives switch closure | every exact cubic claw has a complete finite response classifier | the claw is acyclic and geometry does not decide outside response | unproved; decomposition only |
| high center gives Type B | every divergent pair supplies canonical origin, assignments, safety, and fan provenance | local compatibility does not imply those fields | false automatic identification |
| shared-shoulder failure survives | a failed local fan field can remain on the target-avoiding branch | exact square theorem plus inherited four-cycle exclusion | impossible, now eliminated |
| triangular endpoint failure survives | same | exact square theorem plus inherited four-cycle exclusion | impossible, now eliminated |
| remaining endpoint failure is arbitrary | any endpoint failure remains | the square split forces both ports open | refined to typed open--open residual |

Injection/surjection, pairwise/simultaneous realization, rank/Boolean cube,
representative existence, and context-generator completeness were explicitly
tested.  No such quantifier exchange is used by the new support code.

## Branch and invariant audit

- Positive payment: the new high-pair split pays every shared-shoulder and
  triangular endpoint failure into C1; on the target-avoiding branch those
  constructors are eliminated.
- Negative residual: exactly `PairSurvivor.compatible` or
  `PairSurvivor.openEndpointFailure`, with the latter carrying proofs that both
  ports are open and the first failed endpoint field.
- Measurability: all data come from the two declared ports and their fixed
  two-shoulder rows.
- Ladder legality: level 2.  No new global theorem or ambient enumeration.
- Cross-branch leakage: none in the new split.  The manuscript's later Type-B
  conclusion remains intentionally absent.
- Theorem weakening/assumption: none.  The four-cycle exclusion is an earlier
  field of the exact activated surplus setup, not a new assumption.

## CT obligations

| Instance | Trigger | Unfilled schema | Verdict |
|---|---|---|---|
| High local split | exact distinct ports, degree >=4, deletion criticality, four-cycle exclusion | none for the stated local result | PASS |
| Attachment CT10/CT3 | first mismatch coordinate | token-class table and CT3 trigger | blocking |
| Prefix CT3/C2 | prefix pair | same interface, response completeness, reduction | blocking |
| Cubic CT3/CT7 | cubic decomposition | switch coordinates and complete generator | blocking |
| Type-B handoff | compatible high pair | canonical origin, full assignment, safety, provenance | blocking |

No registered route is claimed by the new support result.

## Reused branches

| Handoff | Trigger | State | Independent? | Verdict |
|---|---|---|---:|---|
| four-cycle C1 | literal `HasCycleWithLength ... FourLength` | same selected graph | yes | exact |
| Type-B ledger | local `FanCompatible` only | incomplete | not applicable | rejected |
| CT3 | path/cubic geometry only | incomplete | not applicable | rejected |

There is no cycle or recursion in this slice.

## Leaf totality

| Leaf | Output | Certificate | Checker | Verdict |
|---|---|---|---|---|
| shared shoulders | C1 four-cycle | literal square | fixed local | closed |
| endpoint failure with triangular first/second | C1 four-cycle | literal square | fixed local | closed |
| open--open endpoint failure | typed finite residual | first failed endpoint field plus two open proofs | <=10 predicates | legal support residual; consumer still open |
| compatible pair | exact local compatibility | three defining fields | <=10 predicates | legal support residual; Type-B trigger still open |
| attachment mismatch | first coordinate | no total consumer | local scan | blocking globally |
| prefix | exact prefix | no total consumer | local | blocking globally |
| cubic | exact decomposition | no semantic consumer | linear construction | blocking globally |

## Practicality

- Largest new universe: two supplied ports, each with a two-vertex shoulder
  list.
- Complexity: ten primitive local predicates; incidence-preserving
  normalization performs no graph search.
- Transfer: the existing named `K_{3,4}` fixture now executes the generic
  `PairSemanticOutcome` classifier and its totality/work theorem.
- No ambient graph/path/context/quotient/coloring family is enumerated.
- No recursion or termination measure is required.

## Formal correspondence and trust

- Exact correspondence: the Lean result matches only the high-separator
  refinement described in the repair ledger; it does not match the full
  manuscript node-[144] conclusion and is not presented as doing so.
- Ownership: generic port and survivor logic is in `Graph`; Erdős code is a thin
  execution on the exact node-[178] predecessor.
- `sorry`/`admit`/new `axiom`/`unsafe`: none in the new files.
- External trust: no new theorem; the kernel-checked graph constructions only.
- Read-only specification: `git diff -- original_erdos_64_proof.tex` is empty.

## Findings

### Blocking

1. Attachment mismatch lacks a token-class semantic table with total outputs.
2. Prefix leaves lack same-interface pieces, response completeness, and a
   certified smaller representative.
3. Cubic leaves lack the finite switch response system and CT3/CT7 consumers.
4. Compatible high pairs lack canonical surplus-slot origin and the complete
   decorated Type-B assignment.
5. Open--open endpoint failures lack their return/chord response classifier.

### Required cleanup

None for the isolated support declarations.  They must remain unmapped in the
web/manuscript node status until the full repair passes.

### Advisory

The detailed separator result should replace, not duplicate, the older
degree-only projection once all downstream consumers use the retained
incidences.  Making that replacement now would be premature because the
semantic consumers are not total.

## FAIL disposition

- First exact obligation returned to the repair loop:
  `attachmentMismatch_classify` from the exact first coordinate and token
  class; the independent prefix/cubic/high-compatible obligations remain as
  listed in the parallel ledger.
- Negated residual: a token-class-tagged first mismatch for which no declared
  local target return fires.
- Methodology level: level-2 finite residual refinement; CT10 followed by CT3
  only when a complete trigger is constructed.
- Successful finer split this iteration: high failures are now exactly square,
  compatible, or open--open endpoint failure; target avoidance removes the
  square constructor.
- Discharged by:
  `classifyPairSemantics`, `classifyPairSurvivor`,
  `SurplusPatternDetailedSeparator.classify`, and the exact root/after-edge
  survivor adapters.
- Audit rerun: yes.  Full verdict remains FAIL.

## Checks

- `lake build StructuralExhaustion.Graph.SurplusPatternDetailedSeparator`
- `lake build StructuralExhaustion.Examples.HighSeparatorPortClassification`
- `lake build Erdos64EG.SemanticBottleneckDetailedSeparator`
- `lake build Erdos64EG.Tests`

All passed.  Existing unrelated linter warnings remain.
