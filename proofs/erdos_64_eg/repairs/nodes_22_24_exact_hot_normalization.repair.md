# Nodes [22]--[24]: exact sequential-hot normalization

## Frozen input and corrected finite statement

The input is the exact node-[21] prefix and the packing-order sequential
hot/cold ledger.  A retained hot owner supplies a complete conditional-fibre
package with

`scaleMultiplicity + scaleLoss = log₂ n`, `scaleLoss ≤ 30`.

The dependent aggregate supplies a total glue on every tuple of retained
local choices, componentwise recovery, response commutation, and an injective
code bounded by the exact labelled-skeleton family.  No Boolean realization
or ambient universe is used.

The manuscript conclusion is asymptotic.  Its correct finite form retains:

1. the sum of all discarded scales; and
2. the positive part of the exact powered skeleton bit count minus the
   printed `1.5 n log₂ n` main term.

These two quantities form `p13SequentialHotNormalizationError`.  The old
finite predicate without this error is not a consequence of the paper's
`+o(1)` statement and is not used as the corrected node-[24] conclusion.

## Kernel-checked route

- `ScaledDyadicRateLower.nat_state_power_lower` converts the bounded exact
  `118108581006 / 10^9` certificate and one local fibre telescope into a
  powered natural-number cardinal inequality.
- `DependentOwnerGlueCapacity.Profile.base_pow_sumWeight_le_codeCard_pow`
  multiplies these ownerwise payments through the recoverable dependent glue
  without materializing its Cartesian domain.
- `P13SequentialHotAggregate.exactPoweredRate_le_fixedCapacity` bounds that
  product by the exact skeleton capacity.
- `p13SequentialHotScaleTotal_add_loss` and
  `p13SequentialHotScaleLoss_le` prove the exact scale partition and the
  thirty-per-owner loss bound.
- `p13SequentialHot_normalized_with_error` is the finite hot inequality.
- `p13SequentialTotal_normalized_le_error_add_cold` combines it with the
  exact hot/cold partition.

Node [22] now decides the corrected total-packing comparison.  Its node-[24]
constructor carries the exact error-bearing cap.  On the strict node-[23]
branch, the total hot/cold inequality proves that the sequential cold list is
nonempty; this is the exact trigger routed to nodes [150]--[153] and the F1--F5
cold analysis.  Node [23] is not closed by an assumed entropy contradiction.

## Locality and termination

All sums range over the exact retained owner enumeration.  Local rate checks
use the fixed 34-row repeated-square certificate.  The dependent product is
used only through cardinality and recovery theorems.  No graph family,
Boolean cube, context family, or ambient vertex universe is enumerated.

## Remaining downstream obligation

To recover the displayed asymptotic coefficient, a later theorem must prove
that the exact skeleton-excess term is `o(n log n)` and use
`scaleLoss ≤ 30` plus `hotCount ≤ n/13` to show that the scale-loss term
is also `o(n log n)`.  This is an asymptotic normalization consumer, not an
input to nodes [22]--[24].
