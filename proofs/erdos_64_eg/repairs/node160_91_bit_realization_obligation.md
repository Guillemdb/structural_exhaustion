# Reserved node [160]: 91-bit realization obligation

This reservation resolves the former provisional node-`[159]` collision.
The cold-frontier numbering is now:

- `[158]`: graph-owned actual thirteen-bit attachment cold fork;
- `[159]`: same-window structural frontier; and
- `[160]`: still-open 91-coordinate completion and realization obligation.

Node `[160]` is **white**.  It must construct the manuscript's actual finite
91-coordinate completion system and prove the required commuting realization
semantics.  It may not be discharged by classifying the distinct thirteen-bit
actual-attachment system, by classical case splitting on an unconstructed
proposition, or by treating the node-`[21]` barrier count rows as a response
map.

Nodes `[158]` and `[159]` do not imply either side of node `[160]`.  They form
a parallel graph-owned continuation and make no 91-bit entropy or density
claim.  This reservation does not edit TeX, WebExport, generated status, or
the implementation log.
