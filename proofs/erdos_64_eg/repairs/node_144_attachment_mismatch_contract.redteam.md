# Red-team audit: node [144] attachment mismatch

## Verdict

The finite mismatch projection is sound.  The branch remains an unmatched
adjacency coordinate because the manuscript's declared target-response
quotient is not constructed by the current predecessor.

## Checks

1. `Origin` retains the actual `FirstHit`, its `holds` proof, and exact
   agreement for every earlier coordinate.
2. The coordinate universe is exactly the declared product of packed-window
   indices, thirteen positions, pair side, and three port roles.
3. Rewriting through `StructuralAttachmentMaps.first_exact` and
   `second_exact` proves that the mismatch is the literal graph adjacency
   asymmetry stated by `mismatch_exact`.
4. The orientation classifier performs only the two relevant adjacency tests
   and returns first-only or second-only.  It does not scan vertices or
   contexts.
5. Equal role vertices would make the two predicates identical, so their
   distinctness is proved unconditionally.
6. `TokenClassOrigin` retains the exact window, remainder, or primitive route
   from the homogeneous audit.  No class is guessed from the mismatch.

## Rejected stronger conclusions

- A single asymmetric incidence does not form a cycle; no return path or chord
  is supplied.
- An adjacency row is not the target-response row of a boundaried quotient.
- Pair inequality does not construct two same-interface pieces.
- Token-class equality records accounting provenance, not target-response
  equivalence.
- No Type-B assignment follows from the mismatch.

## Residual

The exact residual is `UnmatchedCoordinateResidual`: first mismatch,
orientation, distinct role vertices, and token class.  The next repair must
construct the two declared boundaried response coordinates and route their
target behavior through the manuscript's target-defect, compression, or
delocalization alternatives.  Until that construction exists, none of those
exits may be asserted from adjacency inequality alone.
