# Red-team audit: Part I hot/cold local producer

## Baseline

- Repair sketch: `part1_hot_cold_local_producer.md`.
- Manuscript hash:
  `8910b6c91e7c1e9a83a8bec0ce232204f41274d9392df9e9de893f7a4801287f`.
- Failed claim: nodes `[22]`--`[24]`, through the final density ceiling.
- Verdict: **FAIL for full Part I closure; PASS for the corrected
  component-entry producer**.

## Provenance matrix

| Used fact | Earlier producer | Verdict |
|---|---|---:|
| exact CT12 window order | nodes `[17]` and `[21]` | pass |
| fifteen external stubs | node `[152]` arithmetic | pass |
| two transit stubs | inherited incidence order | pass |
| endpoint survives deleted windows | one local membership decision | pass |
| outside-component schedule | graph-owned BFS/filter producer | pass |
| true cyclic successor | exact `List.next` on that schedule | pass |
| shortest component path | graph-owned ordered BFS tree | pass |
| deleted endpoint | typed cross-window residual | pass |

## Quantifier and branch attack

- No coordinatewise response is promoted to simultaneous realization.
- No 13-bit attachment response is identified with the 91 barriers.
- No pairwise compatibility is promoted to global gluing.
- The excess list is literally a drop of two from a proved length-fifteen
  list.
- Every selected incidence is classified exactly once as component-entry or
  cross-window.
- The older F1/F4/F5 split is rejected as manuscript evidence: its return has
  the wrong endpoint, F2/F3 are empty, and F4 is only a high-degree test.

## Leaf totality

| Leaf | Consumer | Verdict |
|---|---|---:|
| component entry | exact D1--D3 observation | pass |
| D1--D3 observation | graph-owned D4 wedge family | pass |
| D4 family | D5--D7 prefix producer | typed; consumer open |
| cross-window | injective oriented-incidence ledger | pass; unordered-edge/gluing consumer open |
| F1--F5 | exact initial-prefix classifier | **blocking** |
| node 24 | density and quarter-budget consumer | **blocking** |

## Practicality and trust

- Largest branch-selection universe: fifteen local incidences.
- Thirteen endpoint decisions per cubic window.
- The decision schedule is bounded by `n`; later component schedule/path work
  uses existing local polynomial bounds.
- No graph, path, context, subgraph, state, or Boolean universe is enumerated.
- Generic transfer and focused Erdős builds pass.

## FAIL disposition

The repair loop receives the exact component-prefix obligation: construct
graph-owned D5--D7 semantics after every exact D4 family and then the
complete F1/F2/F3/F4/F5 split. Only then may bounded overlap be used. The
manuscript remains unchanged and nodes `[22]`--`[24]` remain non-green.

## Follow-up audit

- The cross-window leaf now constructs the exact reverse token and proves
  distinct owners and opposite orientations. The cross-heavy subfamily now
  has an injective aggregate oriented-token ledger and a seven-per-window
  lower bound. An exact scan now returns either reverse closure inside the
  branch-excess subfamily or the first concrete missing reverse. The
  unordered-pair factor-two bound is proved only on the filtered closed
  subledger. The closed outcome has no commuting response gluing, so the
  node-24 verdict does not change. The first missing residual is now refined:
  the exact destination owner and its fifteen-token order yield transit,
  non-cross-heavy/component-heavy, or a branch-excess selection-mismatch
  residual. The mismatch cannot occur: the canonical reverse stub has a
  deleted selected-window endpoint, so its exact local route is cross-window,
  and cross-heavy owner membership inserts it into the selected oriented
  ledger. The remaining missing-reverse outcomes are therefore exactly transit
  and non-cross-heavy/component-heavy; neither is promoted to commuting
  gluing. Aggregation scans only the exact missing-token list. Transit reverse
  labels are injective and lie in the destination owner's literal two-token
  prefix, giving capacity two per owner; every non-transit item projects to its
  exact component-heavy owner. No global token or state universe is introduced.
  The owner fibres cover the transit list and are disjoint by exact owner
  equality, so the global transit count is at most twice the number of actual
  ambient-cubic owners. The complementary non-transit schedule is retained
  separately with its component-heavy proofs.
- D4 now yields a normalized state, not merely a detached evaluator.
- D7 retains only graph-owned sparse-pair supports contained in the active
  component interface and asserts no context Boolean.
- D5 cannot start before a Type A support/receiver-trace producer; D6 cannot
  start before a Type B assigned-support/handoff producer. Treating the
  component path as either support would violate branch provenance.
- Full framework and application builds pass, scoped sources contain no
  `sorry`, `admit`, `axiom`, or `unsafe`, and audited declarations depend only
  on `propext`, `Classical.choice`, and `Quot.sound`.
