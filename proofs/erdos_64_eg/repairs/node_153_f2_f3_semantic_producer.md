# Node [153] F2/F3 finite-response producer

This repair keeps the original stage-major order.  It addresses only the
semantic comparison performed after F1 is false at one exact stored prefix.

## Frozen input and provenance

The input is the node-[152] restricted prefix package, one `Stage` drawn from
its stored ordered schedule, the literal prefix support at that stage, and a
bounded two-boundary exchange on that support.  The exchange owns the source
atom, proposed replacement, identical boundary-degree profile, internal
minimum-degree certificate, target-free certificate, strict local decrease,
and the manuscript's finite response-code table with symbolic coverage of
every compatible outside context.

No statement is imported from a later prefix or a sibling branch.

## Both-sides classification

`Graph.FiniteContextResponseComparison.Profile` runs CT7 on only the supplied
finite response-code alphabet.  Realization is definitionally false because
this execution is the F2/F3 comparison, not a target search.  CT7 therefore
returns exactly one of:

1. a code with unequal response bits; decoding that code gives the literal
   compatible outside context and exact target-response discrepancy required
   by F2; or
2. equality on every code; the symbolic pair-coverage theorem transports that
   equality to every compatible outside context, giving F3 context
   universality.

The second branch combines universality with the stored boundary equality,
baseline, internal target-freeness, and strict decrease.  It constructs
`MinimumDegreeCycleReplacement.Compression.ofTargetComplete` and executes the
public certified CT3 runner, whose terminal is compression and whose verified
outcome contradicts minimality.

## Locality and remaining integration boundary

The work is two passes over the finite local code table.  Outside contexts are
universally quantified in the coverage theorem and are never enumerated.

The restricted prefix package presently stores a prefix support but does not
yet itself construct its literal two-boundary `ProperAtom`, replacement piece,
or D4--D7-complete finite response table.  Consequently
`F2F3StageExchange` makes that exact graph-owned semantic object explicit and
ties it to the stored stage support.  The node-[153] integration must obtain
this object from the F5 repeated/terminal exchange producer; it must not add it
as an author premise or infer it from equality of the coarse cut-state alone.

## Result of the unconditional-producer audit

Attempting to construct the exchange directly from
`P13WeightedColdRestrictedPrefixPackage` reaches an earlier, already typed
residual in the graph layer.  The current computed state is explicitly a
`State (Fin 0)` carrying `MissingD4D7Reconstruction`.  The existing refinement
then proves that complete D4--D7 reconstruction is unavailable at the anchor
row and returns either `CoarseBlocked` or `BoundedBlocked` in
`InducedPathComponentD4D7SemanticReadiness`.

The later clause producers confirm the exact missing inputs rather than merely
failing to expose an API:

- D4 is graph-owned and executable on the actual component support;
- D5 requires a same-support Type-A profile, connectivity, ambient cubicity,
  an internal degree bound, and internal-core freeness; `runCurrent` returns
  the typed `Missing` constructor because none of these is produced on this
  predecessor path;
- D6 has only Type-B subfamily coordinate modules and no complete same-support
  component response producer;
- D7 enumerates actual sparse-pair supports, but its response remains indexed
  by an outside context and there is no finite pair-coverage theorem reducing
  all compatible contexts to those codes.

Therefore equality of the currently computable coarse state cannot be used to
construct F2, F3, or an F5 same-interface germ.  Doing so would reproduce the
false implication that equal displayed/coarse data imply simultaneous exact
response equality.  The paper's stated state includes the complete declared
D4--D7 signature, so the manuscript strategy remains coherent, but the Lean
predecessor does not yet implement that state.  The dependency that must be
implemented before this adapter becomes unconditional is the graph-owned
same-support D5/D6/D7 completion and its finite context-coverage theorem,
followed by literal prefix-to-`ProperAtom`/replacement reconstruction.

## Circularity correction and production order

The production path must not begin with `ColdBoundedGerm`: that object is the
output of the later F5 extraction.  The implemented prefix-side scan now
computes the coarse D1--D3 code directly at each actual restricted prefix and
searches only the literal earlier prefix list.  Its two outputs are:

- no earlier coarse match, which is the exact negative gate for F2 and F3 at
  that stage; or
- an earlier stage, membership in the strict earlier prefix, and equality of
  the two computed coarse codes.

Only the second output may enter D4--D7 refinement.  The finite CT7 bridge
then belongs after literal earlier/current pieces and their complete local
response-code coverage have been constructed.  Universal neutrality plus a
genuine `LexSmaller` proof is F3; universal neutrality without that strict
decrease remains available to F5 and is not called compression.

The generic walk-support profile now derives connectivity, induced-degree
monotonicity, and internal three-core freeness from the actual walk support,
ambient cubicity, and the node-[8] no-proper-core theorem.  This discharges the
structural part of D5 without adding a Type-A ownership assumption.

## Current exact implementation boundary

The D5 carrier-role normalization is now complete.  D7 also has an exact
carrier-local coordinate schedule and a fixed code consisting of the full
role label, the complete 28-role support mask, and the bounded embedded path
value.  The combined D4--D7 projection stores these codes and is constructed
directly from the unique stage-local `LocallyClearStage` payload; there is no
additional Lean outcome or frontier constructor on `[152] -> [153]`.

The obligations that remain are mathematical producers, not proof-flow
cases:

- prove that the locally clear corridor is the manuscript's globally
  surviving corridor, by deriving restricted-high membership in the existing
  sparse-surplus ledger and completeness of the ordinary/decorated/route-8
  producer ledger;
- prove unique branch-ledger provenance for every D6 occurrence entering the
  combined state;
- extend the fixed observed code to the full context-dependent exact response,
  and prove that equal full codes reflect equal exact values;
- feed an actual earlier/current matched pair to the F2 universal-response
  comparison and, only after the F2-negative result, construct the genuine
  strict proper representative required by F3;
- consume an F4 ledger hit only after exact F2- and F3-negative certificates.

The context-dependent D7 target bit is intentionally not inferred from its
embedded carrier-role value.  Consequently the finite exact-value and
equal-code reflection obligations are not marked complete merely from the
fixed D4/D5/D6/D7 carrier codes.
