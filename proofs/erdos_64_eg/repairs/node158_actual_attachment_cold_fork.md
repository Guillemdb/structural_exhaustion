# Proposed node [158]: parallel actual-attachment cold fork

This repair sketch does not edit the source manuscript, proof diagram,
WebExport, generated node status, or implementation log.

## Defect and scope

The original node-`[21]` to node-`[22]` edge remains white: the 91-coordinate
graph-completion family and its Boolean realization theorem are still absent.
Node `[158]` is a parallel finite branch over a different, already graph-owned
system.  It makes no entropy or density claim.

For one literal CT12-selected induced-`P13` window, the actual attachment
system has:

- actual outside vertices as states;
- the thirteen path positions as coordinates; and
- literal graph adjacency as response bits.

The local classifier scans all `2^13` assignments against these actual states.
Its hot constructor is impossible: the all-true assignment would attach one
outside vertex at positions `0` and `2`, and the reusable attachment-cycle
constructor then gives a four-cycle, contradicting target avoidance.

Thus the classifier canonically returns its first missing thirteen-bit
assignment.  Node `[158]` retains that exact cold residual, the node-`[21]`
predecessor, and the selected-window membership proof.  The same selected
window is the typed trigger for the subsequent surplus/corridor node; its
execution and work ledger are deliberately outside node `[158]`.

## Both-sides audit

| Classifier result | Disposition |
|---|---|
| thirteen-bit hot | impossible by the explicit positions-0-and-2 four-cycle |
| thirteen-bit cold | canonical missing assignment plus the exact same-window trigger |

No relation between this missing raw adjacency vector and the missing
91-coordinate completion assignment is asserted.  The later cold corridor
route is a parallel graph-owned continuation available for every selected
window; it is not claimed to be caused by the particular missing vector.

## Practicality

For `n` ambient vertices, the node-`[158]` worst-case reference budget is at most
`2^13 * 13 * n = 106496 n` adjacency-bit comparisons.  The continuation's
path and first-failure work belongs to its subsequent node.  No graph universe,
subgraph universe, completion family, or outside-context universe is
enumerated.

## Merge gate

The Lean fork, hot-impossibility theorem, canonical cold equation, same-window
provenance, route totality, and work bound compile and have received independent
red-team PASS.  Node `[158]` has therefore been synchronized narrowly into TeX
and WebExport.  Nodes `[22]`, `[24]`, and `[159]` remain white regardless of
this fork.
