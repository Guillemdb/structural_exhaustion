# Node [187] review: strong-semantic obligation frontier

## Verdict

**READY FOR INDEPENDENT REVIEW.** Node [187] consumes node [184] exactly and
is green as a typed obligation frontier. It does not prove any of the four
strong semantic conclusions named by the manuscript diagram. Node [190] is
reserved as the sole white consumer that may discharge those obligations.

## Framework-first ownership audit

The node-[184] payload contains only:

- exact mismatch or prefix evidence;
- an exact cubic `SwitchBoundaryShape` and support equality; or
- the selected high centre's exact declared port schedule, cardinality, and
  lower bound four.

There is no producer for a sparse-exit theorem, compatible response map, CT3
compression input, Type-B fan-safety certificate, or fixed-capacity theorem.
Consequently node [187] may route these as missing obligations but cannot
honestly return them as established conclusions.

Reusable obligation and exact-payload retention live in
`Graph.LocalSeparatorSemanticFrontier`. The surplus-specific seven-constructor
routing lives in `Graph.SurplusPatternStrongSemanticFrontier`. The Erdős file
is an exact source-gated instantiation.

## Exact predecessor and branch ledger

`SemanticBottleneckStrongFrontierSource.node184Exact` equates the stored
predecessor with `semanticBottleneckLocalProjection` on the canonical
node-[184] source. The result is indexed by `source.node184.projection`, and
`pendingExact` equates it with the reusable classifier on that same value.
`retainedExact` then proves that the pending payload is literally the node-184
projection.

The one-constructor local routing table is:

| exact node-[184] constructor | retained data | pending obligation |
|---|---|---|
| attachment mismatch | exact tag equality and evidence | sparse exit |
| aligned left prefix | exact tag equality and evidence | fixed caps |
| aligned right prefix | exact tag equality and evidence | fixed caps |
| cubic root | exact cubic projection | CT3 |
| high root | exact high-port projection | Type B |
| cubic after edge | exact cubic projection | CT3 |
| high after edge | exact high-port projection | Type B |

An `Obligation` is only a routing tag. `Pending` contains no field asserting
that its tag is true, realizable, sufficient, or terminal. Thus the table does
not turn mismatch into a sparse exit, a cubic boundary into response
equivalence, or a high degree into fan safety.

## Local computation

The classifier inspects one already-computed node-[184] constructor and copies
its payload. Its visible work is the constant one. It enumerates no vertices,
ports, pairs, contexts, response functions, colorings, graphs, types, or other
ambient universes. In particular, the high-centre port enumeration was already
materialized and certified by node [184]; node [187] does not rescan it.

## Non-Erdős transfer

`Examples.SurplusPatternStrongSemanticFrontier` runs the reusable profile on:

- the branching-tree cubic projection, checking exact payload retention and
  the pending CT3 tag; and
- the `K5` high-centre projection, checking exact payload retention and the
  pending Type-B tag.

The examples intentionally prove no CT3 or Type-B semantic statement.

## Validation and trust

Focused builds pass:

```text
lake build StructuralExhaustion.Graph.SurplusPatternStrongSemanticFrontier \
  StructuralExhaustion.Examples.SurplusPatternStrongSemanticFrontier
lake build Erdos64EG.SemanticBottleneckStrongFrontier
```

The scoped source scan finds no `sorry`, `admit`, `unsafe`, new axiom,
`Finset.univ`, or `Set.univ`; diff checks are clean. `#print axioms` reports no
axioms for the generic `pending` constructor, standard Lean axioms only for the
dependent graph/Erdős classifiers, and the single permitted inherited HSS
axiom only at the official prefix endpoint.

## Integration boundary

The shared umbrella imports, WebExport, topology, TeX, README, and
implementation log are intentionally untouched for concurrent integration.
When integrated, the TeX/web label must describe node [187] as a pending
obligation frontier rather than claiming the four semantic conclusions. The
only outgoing edge should target white node [190].

## Independent cross-review

An independent no-yellow audit confirms **PASS** without a theorem-bearing
repair.

- `SemanticBottleneckStrongFrontierSource.node184Exact` fixes the predecessor
  to the actual integrated node-184 runner.  The dependent `pending` field is
  built from that exact projection, and `pendingExact` identifies it with the
  reusable classifier on the same value.
- `required` pattern matches all seven node-184 constructors exactly once.
  Its reduction table is precisely: attachment mismatch -> `sparseExit`;
  either aligned prefix -> `fixedCaps`; either cubic switch -> `ct3`; either
  high separator -> `typeB`.  No default or catch-all branch exists.
- `Pending` contains only `retained`, equality with the supplied payload,
  `obligation`, and equality with the required tag.  It contains no witness,
  realization, soundness field, terminal, response map, quotient, capacity
  ledger, fan certificate, or target theorem.  Therefore the exact tag mapping
  is routing metadata and cannot be eliminated as a certificate for the named
  obligation.
- `classify_retains` proves literal node-184 payload preservation, while
  `classify_obligation_exact` proves only tag equality.  The Erdős wrapper
  retains both the exact source and equality with this classifier.
- The classifier performs one constructor inspection and copies the payload.
  Its visible work is at most one; it does not rescan the high-port schedule or
  enumerate vertices, ports, pairs, contexts, responses, graphs, or colorings.
- The non-Erdős transfer checks the canonical cubic payload with pending CT3
  and the canonical high payload with pending Type B.  These examples inspect
  retention and tags only and deliberately expose no semantic conclusion.
- Focused builds and trust checks pass.  The generic `pending` constructor is
  axiom-free; dependent classifiers use only `propext`, `Classical.choice`, and
  `Quot.sound`; the official prefix endpoint inherits exactly the permitted
  HSS theorem from prior stages.

No sparse-exit theorem, CT3 response or compression, Type-B structure,
fixed-capacity conclusion, target behavior, or branch closure appears in the
reviewed declarations.  Node [190] is the sole white successor and must
separately prove any such statement.
