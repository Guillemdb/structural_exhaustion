# EG Node 7 Migration Packet

Date: 2026-07-22

Status: direct CT1 C1 terminal checked; semantic parity baseline pending

## Authority Snapshot

The sole mathematical and DAG authority is
`original_erdos_64_proof.tex`, pinned by
`migration/hypostructure/source-authority.json` at SHA-256
`215248521b7dbc6519076d506101b80a1c9a8425ee5c13b48a4ec52df21d2e10`.
The living proof is not an obligation source.

The frozen original statements are:

- diagram node `[7]`: `power-of-two cycle` terminal (original line 577);
- direct incoming edge: `[6] -- yes -> [7]` (original line 601);
- no outgoing residual edge: node `[7]` is a terminal;
- table item `[5]--[7]`: edge-rooted target equivalence, no `C_{2^j}`
  iff all `R_e(G)\cap\Mers=\varnothing`, with a Mersenne return giving a
  target cycle (original line 1093);
- `\cref{lem:return-equivalence}`: a return of length `2^j - 1` together
  with its root edge gives a simple cycle of length `2^j` (original
  lines 1651-1666);
- framework branch table: the Mersenne-return branch has no residual demand
  and is closed by `\cref{lem:return-equivalence}` (original
  lines 1733-1747).

Node 7 performs only the terminal closure of the C1 branch generated by
node 6. The avoiding branch remains node 6's no-return residual and is
consumed by node 8.

## Mandatory Contract

| Field | Frozen value |
|---|---|
| Node ID | 7 |
| Literal predecessor | `Node6Stage` |
| Incoming branch | Node 6 C1 terminal, `[6] -- yes -> [7]` |
| Incoming queries | `node6RouteQuery`, `node6ObjectQuery` |
| Inherited facts | CT1 generated C1 certificate and selected graph |
| Local responsibility | Decode the C1 target as a power-of-two cycle terminal |
| New payload | `Core.Closure.Result` for the public target |
| Executor | `CT1.FocusedCertificateEncoding.target_of_c1` plus `Core.Closure.Result.direct` |
| Complementary residuals | None for node 7; node 6 avoiding arm is consumed by node 8 |
| Outgoing consumers | Terminal only |

The legal state flow is:

```text
Node6Stage
  -> Node6Focus active branch
  -> node6RouteQuery reads CT1's generated route
  -> C1 terminal equality
  -> CT1.FocusedCertificateEncoding.target_of_c1
  -> Core.Closure.Result.direct
```

## Provision Boundary

| Input | Role | Reason |
|---|---|---|
| `node7_powerOfTwoCycle` | semantic law | Instantiates the original return-equivalence target terminal through CT1's public C1 decoder |
| `isC1` | predecessor branch fact | Identifies the exact framework-selected C1 arm from node 6 |
| `node6RouteQuery` | predecessor projection | Reads the framework-generated route; it is not copied into a node-local output |

Framework-generated values are kept separate:

- `node6RouteQuery` exposes CT1's route from the predecessor;
- `CT1.FocusedCertificateEncoding.target_of_c1` decodes the target;
- `node7` wraps the target in `Core.Closure.Result.direct`;
- `node7WorkBudget` records no additional primitive search; and
- `node7Metadata` records no manual obligations.

## Legacy Difference

The legacy node 7 closes the C1 branch by contradiction against the inherited
node-5 avoidance theorem and then exposes only the avoiding successor used by
node 8. The Hypostructure-native migration separates these responsibilities:
node 7 is the literal terminal target closure, while node 6's framework-owned
`closeC1ContinueAvoidingCounted` produces the node-8 avoiding residual.

The parity module records both normalized facts:

- a new C1 branch exposes the public `Target` and closes directly; and
- the legacy C1 terminal has the same target-contradiction shape.

## Completion Gates

| Gate | Evidence |
|---|---|
| Kernel | `lake env lean HypostructureErdos64EG/Node7.lean` and `lake build HypostructureErdos64EG.Node7` |
| Parity | `examples/hypostructure_parity/HypostructureParity/Erdos64EG/Node7.lean` checks; clean semantic baseline remains pending |
| Mathematics | Closed: the exact node-7 responsibility is CT1 C1 target decoding plus direct terminal closure |
| Work | Captured by `node7WorkBudget` and `node7_work_bounded` |
| Trust | `#print axioms` reports only `propext`, `Classical.choice`, and `Quot.sound` |
| Web | `generated/hypostructure/web/snapshot.json` after `make web-data` |

Node 8 is the next dependency-ready packet on the no-return branch.
