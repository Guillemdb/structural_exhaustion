# Migration decisions

Add a numbered record whenever a missing use case changes one of the three API
specifications or whenever the frozen baseline is refreshed. Use
`0000-template.md`; allocate the next stable four-digit identifier; and keep
the record focused on one ownership or compatibility decision.

A decision is not implementation evidence. Link the matrix rows and fixtures
that execute it, and leave their status unchanged until the corresponding
checks pass.

