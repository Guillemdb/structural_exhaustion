{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/structural-exhaustion/concrete/nodes/CT14.N08.schema.json",
  "title": "CT14.N08 completed discharge record — Payload record — P_14→9: unbounded-capacity member",
  "description": "Concrete completed-discharge schema for CT14.N08, the residual record node “Payload record — P_14→9: unbounded-capacity member” of tactic CT14 (Aggregate closure). It refines the common node-proof record with this node's exact identity, completion state, assigned contract evidence, and any required payload or terminal certificate.",
  "allOf": [
    {
      "$ref": "../../structural-exhaustion.bundle.schema.json#/$defs/nodeProofRecord"
    },
    {
      "properties": {
        "tacticId": {
          "description": "Framework tactic discriminator for CT14.N08. It must be exactly `CT14`, selecting Aggregate closure (aggregate) as the tactic that owns the residual record step “Payload record — P_14→9: unbounded-capacity member” in the exhaustive proof flow.",
          "const": "CT14"
        },
        "nodeId": {
          "description": "Stable graph-node identifier for the obligation “Payload record — P_14→9: unbounded-capacity member”. It must be exactly `CT14.N08` so the evidence, dependencies, outgoing branch, and any terminal result are attributed to this step rather than another node.",
          "const": "CT14.N08"
        },
        "nodeNumber": {
          "description": "One-based ordinal of CT14.N08 within tactic CT14 (Aggregate closure). It must be exactly 8, matching the N08 suffix and the node's position in that tactic graph.",
          "const": 8
        },
        "status": {
          "description": "Required completion state for CT14.N08. It must be exactly `defined`, meaning that the required definition or recorded branch-state object has been supplied for the residual record obligation “Payload record — P_14→9: unbounded-capacity member”; another lifecycle value does not discharge this concrete node.",
          "const": "defined"
        },
        "dischargedContracts": {
          "description": "Contract identifiers whose proof obligations are discharged at CT14.N08. The array must contain `S-Def`; these are precisely the contracts assigned to “Payload record — P_14→9: unbounded-capacity member”. Listing an identifier records discharge status but does not replace the matching typed evidence record required in `contractInstances`.",
          "allOf": [
            {
              "contains": {
                "description": "Requires `S-Def` to occur in CT14.N08's discharged-contract list, because this node must use the contract that constructs a typed object deterministically from recorded branch-state data for the obligation “Payload record — P_14→9: unbounded-capacity member”.",
                "const": "S-Def"
              }
            }
          ]
        },
        "contractInstances": {
          "description": "Typed contract-evidence records that justify CT14.N08 from the recorded branch state. The array must contain at least one instance for every assigned schema: S-Def (subject, sourceStateFields, constructor, parameters, evidence). Every instance must include its schema discriminator and nonempty evidence as required by the referenced contract definition; together the records must establish the displayed node statement.",
          "allOf": [
            {
              "contains": {
                "required": [
                  "schema"
                ],
                "properties": {
                  "schema": {
                    "description": "Contract discriminator for a `S-Def` evidence instance at CT14.N08. It must be exactly `S-Def`, identifying the contract that constructs a typed object deterministically from recorded branch-state data; the same instance must supply subject, sourceStateFields, constructor, parameters, evidence.",
                    "const": "S-Def"
                  }
                }
              }
            }
          ]
        }
      },
      "required": [
        "tacticId",
        "nodeId",
        "nodeNumber",
        "status",
        "dischargedContracts",
        "contractInstances"
      ]
    }
  ],
  "x-obligation": {
    "obligationId": "CT14.N08.O1",
    "modality": "define",
    "statement": {
      "informal": "Define or verify: Payload record — P_14→9: unbounded-capacity member",
      "latex": "\\textbf{Payload record}\\\\\\(P_{14\\to9}\\): unbounded-capacity member"
    },
    "contractSchemas": [
      "S-Def"
    ],
    "dependencies": [
      "CT14.N05"
    ],
    "dischargeCriteria": "Provide a proof/definition/computation artifact that establishes the displayed statement from the recorded branch state and satisfies every listed contract schema."
  }
}
