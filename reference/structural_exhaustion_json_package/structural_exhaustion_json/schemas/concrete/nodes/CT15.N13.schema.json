{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/structural-exhaustion/concrete/nodes/CT15.N13.schema.json",
  "title": "CT15.N13 completed discharge record — CT 7 — exchange",
  "description": "Concrete completed-discharge schema for CT15.N13, the consumer handoff node “CT 7 — exchange” of tactic CT15 (Rank forcing). It refines the common node-proof record with this node's exact identity, completion state, assigned contract evidence, and any required payload or terminal certificate.",
  "allOf": [
    {
      "$ref": "../../structural-exhaustion.bundle.schema.json#/$defs/nodeProofRecord"
    },
    {
      "properties": {
        "tacticId": {
          "description": "Framework tactic discriminator for CT15.N13. It must be exactly `CT15`, selecting Rank forcing (rank) as the tactic that owns the consumer handoff step “CT 7 — exchange” in the exhaustive proof flow.",
          "const": "CT15"
        },
        "nodeId": {
          "description": "Stable graph-node identifier for the obligation “CT 7 — exchange”. It must be exactly `CT15.N13` so the evidence, dependencies, outgoing branch, and any terminal result are attributed to this step rather than another node.",
          "const": "CT15.N13"
        },
        "nodeNumber": {
          "description": "One-based ordinal of CT15.N13 within tactic CT15 (Rank forcing). It must be exactly 13, matching the N13 suffix and the node's position in that tactic graph.",
          "const": 13
        },
        "status": {
          "description": "Required completion state for CT15.N13. It must be exactly `validated`, meaning that the node's certificate, trigger, or other validation obligation has been checked for the consumer handoff obligation “CT 7 — exchange”; another lifecycle value does not discharge this concrete node.",
          "const": "validated"
        },
        "dischargedContracts": {
          "description": "Contract identifiers whose proof obligations are discharged at CT15.N13. The array must contain `S-Trig`; these are precisely the contracts assigned to “CT 7 — exchange”. Listing an identifier records discharge status but does not replace the matching typed evidence record required in `contractInstances`.",
          "allOf": [
            {
              "contains": {
                "description": "Requires `S-Trig` to occur in CT15.N13's discharged-contract list, because this node must use the contract that checks that a payload supplies the receiving tactic's trigger, arity, interfaces, and standing hypotheses for the obligation “CT 7 — exchange”.",
                "const": "S-Trig"
              }
            }
          ]
        },
        "contractInstances": {
          "description": "Typed contract-evidence records that justify CT15.N13 from the recorded branch state. The array must contain at least one instance for every assigned schema: S-Trig (payloadId, consumerTacticId, expectedArity, componentChecks, standingHypotheses, interfaceChecks, certificateHandleChecks, evidence). Every instance must include its schema discriminator and nonempty evidence as required by the referenced contract definition; together the records must establish the displayed node statement.",
          "allOf": [
            {
              "contains": {
                "required": [
                  "schema"
                ],
                "properties": {
                  "schema": {
                    "description": "Contract discriminator for a `S-Trig` evidence instance at CT15.N13. It must be exactly `S-Trig`, identifying the contract that checks that a payload supplies the receiving tactic's trigger, arity, interfaces, and standing hypotheses; the same instance must supply payloadId, consumerTacticId, expectedArity, componentChecks, standingHypotheses, interfaceChecks, certificateHandleChecks, evidence.",
                    "const": "S-Trig"
                  }
                }
              }
            }
          ]
        }
      },
      "required": [
        "tacticId",
        "nodeId",
        "nodeNumber",
        "status",
        "dischargedContracts",
        "contractInstances"
      ]
    }
  ],
  "x-obligation": {
    "obligationId": "CT15.N13.O1",
    "modality": "validate",
    "statement": {
      "informal": "Validate or verify: CT 7 — exchange",
      "latex": "\\textbf{CT 7}\\\\exchange"
    },
    "contractSchemas": [
      "S-Trig"
    ],
    "dependencies": [
      "CT15.N10"
    ],
    "dischargeCriteria": "Provide a proof/definition/computation artifact that establishes the displayed statement from the recorded branch state and satisfies every listed contract schema."
  }
}
