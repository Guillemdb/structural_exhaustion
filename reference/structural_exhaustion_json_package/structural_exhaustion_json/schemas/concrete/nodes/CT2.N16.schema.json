{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/structural-exhaustion/concrete/nodes/CT2.N16.schema.json",
  "title": "CT2.N16 completed discharge record — Payload lemma — build P_2→10 — criticality refinement",
  "description": "Concrete completed-discharge schema for CT2.N16, the payload constructor node “Payload lemma — build P_2→10 — criticality refinement” of tactic CT2 (Minimality and replacement). It refines the common node-proof record with this node's exact identity, completion state, assigned contract evidence, and any required payload or terminal certificate.",
  "allOf": [
    {
      "$ref": "../../structural-exhaustion.bundle.schema.json#/$defs/nodeProofRecord"
    },
    {
      "properties": {
        "tacticId": {
          "description": "Framework tactic discriminator for CT2.N16. It must be exactly `CT2`, selecting Minimality and replacement (minimality) as the tactic that owns the payload constructor step “Payload lemma — build P_2→10 — criticality refinement” in the exhaustive proof flow.",
          "const": "CT2"
        },
        "nodeId": {
          "description": "Stable graph-node identifier for the obligation “Payload lemma — build P_2→10 — criticality refinement”. It must be exactly `CT2.N16` so the evidence, dependencies, outgoing branch, and any terminal result are attributed to this step rather than another node.",
          "const": "CT2.N16"
        },
        "nodeNumber": {
          "description": "One-based ordinal of CT2.N16 within tactic CT2 (Minimality and replacement). It must be exactly 16, matching the N16 suffix and the node's position in that tactic graph.",
          "const": 16
        },
        "status": {
          "description": "Required completion state for CT2.N16. It must be exactly `proved`, meaning that the node's mathematical assertion or construction has been established for the payload constructor obligation “Payload lemma — build P_2→10 — criticality refinement”; another lifecycle value does not discharge this concrete node.",
          "const": "proved"
        },
        "dischargedContracts": {
          "description": "Contract identifiers whose proof obligations are discharged at CT2.N16. The array must contain `S-Rout`; these are precisely the contracts assigned to “Payload lemma — build P_2→10 — criticality refinement”. Listing an identifier records discharge status but does not replace the matching typed evidence record required in `contractInstances`.",
          "allOf": [
            {
              "contains": {
                "description": "Requires `S-Rout` to occur in CT2.N16's discharged-contract list, because this node must use the contract that packages a typed payload and verifies its unique producer-to-consumer route for the obligation “Payload lemma — build P_2→10 — criticality refinement”.",
                "const": "S-Rout"
              }
            }
          ]
        },
        "contractInstances": {
          "description": "Typed contract-evidence records that justify CT2.N16 from the recorded branch state. The array must contain at least one instance for every assigned schema: S-Rout (payloadId, producerTacticId, consumerTacticId, tupleEntries, vocabularyVerified, uniqueConsumerVerified, evidence). Every instance must include its schema discriminator and nonempty evidence as required by the referenced contract definition; together the records must establish the displayed node statement.",
          "allOf": [
            {
              "contains": {
                "required": [
                  "schema"
                ],
                "properties": {
                  "schema": {
                    "description": "Contract discriminator for a `S-Rout` evidence instance at CT2.N16. It must be exactly `S-Rout`, identifying the contract that packages a typed payload and verifies its unique producer-to-consumer route; the same instance must supply payloadId, producerTacticId, consumerTacticId, tupleEntries, vocabularyVerified, uniqueConsumerVerified, evidence.",
                    "const": "S-Rout"
                  }
                }
              }
            }
          ]
        }
      },
      "required": [
        "tacticId",
        "nodeId",
        "nodeNumber",
        "status",
        "dischargedContracts",
        "contractInstances"
      ]
    },
    {
      "properties": {
        "producedPayloads": {
          "description": "Typed routing outputs constructed by CT2.N16. The array must contain `P_2_to_10`; these payload identifiers carry the data certified by “Payload lemma — build P_2→10 — criticality refinement”. The required consumer routes are `P_2_to_10` → CT10. Their construction and consumer compatibility must be justified by the node's `S-Rout` contract instance.",
          "allOf": [
            {
              "contains": {
                "description": "Requires payload `P_2_to_10` among the outputs of CT2.N16; this is the typed handoff constructed by “Payload lemma — build P_2→10 — criticality refinement” for CT10.",
                "const": "P_2_to_10"
              }
            }
          ]
        }
      },
      "required": [
        "producedPayloads"
      ]
    }
  ],
  "x-obligation": {
    "obligationId": "CT2.N16.O1",
    "modality": "construct",
    "statement": {
      "informal": "Construct or verify: Payload lemma — build P_2→10 — criticality refinement",
      "latex": "\\textbf{Payload lemma}\\\\build \\(P_{2\\to10}\\)\\\\criticality refinement"
    },
    "contractSchemas": [
      "S-Rout"
    ],
    "dependencies": [
      "CT2.N15"
    ],
    "dischargeCriteria": "Provide a proof/definition/computation artifact that establishes the displayed statement from the recorded branch state and satisfies every listed contract schema."
  }
}
