{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/structural-exhaustion/concrete/nodes/CT16.N05.schema.json",
  "title": "CT16.N05 completed discharge record — Definition — exact closed type for the whole object",
  "description": "Concrete completed-discharge schema for CT16.N05, the definition node “Definition — exact closed type for the whole object” of tactic CT16 (Whole-object exact types). It refines the common node-proof record with this node's exact identity, completion state, assigned contract evidence, and any required payload or terminal certificate.",
  "allOf": [
    {
      "$ref": "../../structural-exhaustion.bundle.schema.json#/$defs/nodeProofRecord"
    },
    {
      "properties": {
        "tacticId": {
          "description": "Framework tactic discriminator for CT16.N05. It must be exactly `CT16`, selecting Whole-object exact types (exact type) as the tactic that owns the definition step “Definition — exact closed type for the whole object” in the exhaustive proof flow.",
          "const": "CT16"
        },
        "nodeId": {
          "description": "Stable graph-node identifier for the obligation “Definition — exact closed type for the whole object”. It must be exactly `CT16.N05` so the evidence, dependencies, outgoing branch, and any terminal result are attributed to this step rather than another node.",
          "const": "CT16.N05"
        },
        "nodeNumber": {
          "description": "One-based ordinal of CT16.N05 within tactic CT16 (Whole-object exact types). It must be exactly 5, matching the N05 suffix and the node's position in that tactic graph.",
          "const": 5
        },
        "status": {
          "description": "Required completion state for CT16.N05. It must be exactly `defined`, meaning that the required definition or recorded branch-state object has been supplied for the definition obligation “Definition — exact closed type for the whole object”; another lifecycle value does not discharge this concrete node.",
          "const": "defined"
        },
        "dischargedContracts": {
          "description": "Contract identifiers whose proof obligations are discharged at CT16.N05. The array must contain `S-Def`; these are precisely the contracts assigned to “Definition — exact closed type for the whole object”. Listing an identifier records discharge status but does not replace the matching typed evidence record required in `contractInstances`.",
          "allOf": [
            {
              "contains": {
                "description": "Requires `S-Def` to occur in CT16.N05's discharged-contract list, because this node must use the contract that constructs a typed object deterministically from recorded branch-state data for the obligation “Definition — exact closed type for the whole object”.",
                "const": "S-Def"
              }
            }
          ]
        },
        "contractInstances": {
          "description": "Typed contract-evidence records that justify CT16.N05 from the recorded branch state. The array must contain at least one instance for every assigned schema: S-Def (subject, sourceStateFields, constructor, parameters, evidence). Every instance must include its schema discriminator and nonempty evidence as required by the referenced contract definition; together the records must establish the displayed node statement.",
          "allOf": [
            {
              "contains": {
                "required": [
                  "schema"
                ],
                "properties": {
                  "schema": {
                    "description": "Contract discriminator for a `S-Def` evidence instance at CT16.N05. It must be exactly `S-Def`, identifying the contract that constructs a typed object deterministically from recorded branch-state data; the same instance must supply subject, sourceStateFields, constructor, parameters, evidence.",
                    "const": "S-Def"
                  }
                }
              }
            }
          ]
        }
      },
      "required": [
        "tacticId",
        "nodeId",
        "nodeNumber",
        "status",
        "dischargedContracts",
        "contractInstances"
      ]
    }
  ],
  "x-obligation": {
    "obligationId": "CT16.N05.O1",
    "modality": "define",
    "statement": {
      "informal": "Define or verify: Definition — exact closed type for the whole object",
      "latex": "\\textbf{Definition}\\\\exact closed type for the whole object"
    },
    "contractSchemas": [
      "S-Def"
    ],
    "dependencies": [
      "CT16.N02"
    ],
    "dischargeCriteria": "Provide a proof/definition/computation artifact that establishes the displayed statement from the recorded branch state and satisfies every listed contract schema."
  }
}
