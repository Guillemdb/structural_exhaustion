{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/structural-exhaustion/concrete/nodes/CT1.terminal.scope.schema.json",
  "title": "CT1.terminal.scope completed discharge record — scope terminal",
  "description": "Concrete completed-discharge schema for semantic CT1-v1 node CT1.terminal.scope.",
  "allOf": [
    {
      "$ref": "../../structural-exhaustion.bundle.schema.json#/$defs/nodeProofRecord"
    },
    {
      "properties": {
        "tacticId": {
          "description": "Owning tactic discriminator for CT1.terminal.scope; it must be CT1.",
          "const": "CT1"
        },
        "nodeId": {
          "description": "Stable semantic runtime identifier for scope terminal.",
          "const": "CT1.terminal.scope"
        },
        "nodeNumber": {
          "description": "One-based presentation-order index for CT1.terminal.scope.",
          "const": 3
        },
        "status": {
          "description": "Required successful validation state for this entry or terminal node.",
          "const": "validated"
        },
        "dischargedContracts": {
          "type": "array",
          "uniqueItems": true,
          "minItems": 1,
          "maxItems": 1,
          "description": "Exact contract families discharged at CT1.terminal.scope: A-Scope.",
          "allOf": [
            {
              "contains": {
                "description": "Requires A-Scope, which records the typed scope obstruction.",
                "const": "A-Scope"
              }
            }
          ]
        },
        "contractInstances": {
          "type": "array",
          "minItems": 1,
          "description": "Typed contract instances for CT1.terminal.scope; every declared family is required.",
          "allOf": [
            {
              "contains": {
                "required": [
                  "schema"
                ],
                "properties": {
                  "schema": {
                    "description": "Discriminator for A-Scope evidence at CT1.terminal.scope; it records the typed scope obstruction.",
                    "const": "A-Scope"
                  }
                }
              }
            }
          ]
        }
      },
      "required": [
        "tacticId",
        "nodeId",
        "nodeNumber",
        "status",
        "dischargedContracts",
        "contractInstances"
      ]
    }
  ],
  "x-obligation": {
    "obligationId": "CT1.terminal.scope.O1",
    "modality": "audit",
    "statement": {
      "informal": "The semantic run ends at the exact scope outcome.",
      "latex": ""
    },
    "contractSchemas": [
      "A-Scope"
    ],
    "dependencies": [
      "CT1.decide.scope"
    ],
    "dischargeCriteria": "The terminal constructor, raw outcome, certified outcome, and incoming typed edge all resolve."
  }
}
