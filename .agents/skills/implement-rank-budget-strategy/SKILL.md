---
name: implement-rank-budget-strategy
description: Implement a reusable Hypostructure rank-and-budget strategy. Use for finite target-relative rank, entropy or threshold splits, bounded scale thickening, survivor arithmetic, coordinate dependence, and graph charge or PDE energy alternatives.
---

# Implement a Rank and Budget Strategy

Read coordinates, thresholds, scales, offsets, and budget values from predecessor queries. Keep the budget carrier abstract and use only its registered comparison and aggregation laws.

Compose rank classification, first dependence, high/low budget routing, bounded compatibility scans, and survivor arithmetic. Preserve exact coordinate and scale witnesses in the ledger.

Expose one registered DAG vertex with every branch: dependent coordinate, full-rank ledger, high budget, low budget, target hit, finite survivor, or remaining residual. Include finite coverage and work bounds; never hardcode application constants in Core.

Read only the previous residual and accumulated ledger. Keep the output exhaustive and route missing public machinery through `extend-hypostructure-framework`.
