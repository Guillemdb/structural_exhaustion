---
name: implement-closed-code-strategy
description: Implement a reusable Hypostructure closed-code exhaustion strategy. Use for whole-support finite code scans, first missing coordinates, exact target-code equality or mismatch, closed classes, repeated codes, and certified removal continuations.
---

# Implement a Closed Code Strategy

Consume one complete coordinate schedule from the predecessor. Audit support before computing the code, compute the closed code exactly once, and compare it literally with the target code.

Compose support completion, code equality/mismatch, repetition classification, and removal or continuation as one strategy. Core owns first-hit selection and routing; the domain supplies only code semantics and closure laws.

Expose one registered DAG vertex returning first missing coordinate, target code, mismatch, repeated class, certified removal, or exact residual. Prove schedule completeness and avoid symbolic expansion of the represented object.

Read only the previous residual and accumulated ledger. Keep the output exhaustive and route missing public machinery through `extend-hypostructure-framework`.
