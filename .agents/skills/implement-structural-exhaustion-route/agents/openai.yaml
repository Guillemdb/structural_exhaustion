interface:
  display_name: "Structural Exhaustion CT Transitions"
  short_description: "Implement full-ledger CT transitions"
  default_prompt: "Use $implement-structural-exhaustion-route to implement this transition entirely inside the framework and expose one full-ledger node executor."
