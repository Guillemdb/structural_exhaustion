---
name: implement-response-classification-strategy
description: Implement a reusable Hypostructure finite-response classification strategy. Use for exact context responses, first distinguishing coordinates, known versus novel classes, repeated types, response-neutral continuations, and refinement tables over a residual-owned schedule.
---

# Implement a Response Classification Strategy

Read representatives, contexts, and coordinate schedules from the previous residual and ledger. Compute responses through registered symbolic queries; never unfold ambient objects or enumerate object families.

Compose exact response observation, deterministic first distinction, class lookup, repetition, and continuation routing as one strategy. Preserve the representative and context witnesses selected by Core.

Every outcome must be typed and exhaustive: target realization, distinguishing response, known class transport, novel class residual, neutral continuation, or certified repetition/removal. Prove the finite schedule covers the semantic comparison being claimed.

Expose the classifier as a registered DAG vertex. Add missing generic response machinery in Core and domain semantics in Graph/PDE, never in the application DAG.

Read only the previous residual and accumulated ledger. If the public constructor is missing, use `extend-hypostructure-framework`; keep every terminal exhaustive.
