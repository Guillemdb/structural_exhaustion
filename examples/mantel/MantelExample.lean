import MantelExample.Run
import MantelExample.Concrete
import MantelExample.CT10EdgeAttachment
import MantelExample.K34OpenPortSuppression
import MantelExample.CT15EdgeResponses
import MantelExample.CT9EndpointRoleLedger
import MantelExample.GreedyMatchingStar
import MantelExample.ConditionalFibreProductCost
import MantelExample.FiniteStateEntropyBookkeeping
import MantelExample.FinitePowerScaleSplit
import MantelExample.FiniteJointCapacity
import MantelExample.FinitePoweredBudgetTransfer

/-! External-package entry point for the Mantel CT10/CT11 benchmark and the
`K₃,₄` open-port suppression transfer check, exact CT15 responses, the
exact CT9 endpoint--role ledger on the textbook edge `K₂`, and the shared
greedy matching--star scan on the six edges of `K₄`, and exact local
finite-state bookkeeping, constant-work power-scale split, and zero-scan
joint-capacity and symbolic powered-budget transfers on the supplied vertices
and darts of `C₅`. -/
